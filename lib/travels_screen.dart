
import 'package:flutter/material.dart';

class TravelsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: Text('Travels'),
        ),
      body: Text(
        'Thanks For Joining Trav.'
      ),
      );
  }

}